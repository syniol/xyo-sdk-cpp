#include <xyo/client.hpp>
#include <iostream>

int main() {
    // Basic verification of symbols and linking (no active network calls to avoid external dependency issues)
    std::cout << "[Test Package] XYO SDK headers and library linked successfully!\n";
    return 0;
}
